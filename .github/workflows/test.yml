name: Test Workflow

on:
  workflow_dispatch:
    inputs:
      IS_LATEST:
        description: 'latest'
        required: true
        type: boolean
        default: false


jobs:
  demo-job:
    runs-on: ubuntu-20.04
    steps:
      - name: Test
        run: |
          if [[ "${{github.event.inputs.IS_LATEST}}" == "true" ]]; then
            echo "WORKS"
          fi

          
      